[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "parser-domclick"
version = "0.0.0"
description = "Parser of Domclick"
readme = "README.md"
requires-python = ">=3.11"
authors = [ { name = "Ho1ocron" } ]
